@model Web.Iot.PortalService.Models.ViewModels.MonitorModel

@{
    ViewData["Title"] = "Monitor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="container">
        <h3 class="h3">Setting Service</h3>
        <div class="container row">
            <p class="font-weight-bold">Service Health:  </p>
            <br />
            <p class="font-weight-normal">@Model.SettingServiceHealth</p>
        </div>
        @if (Model.IsSettingServiceHealthy)
        {
            <div class="container row">
                <p class="font-weight-bold">Setting Count:  </p>
                <br />
                <p class="font-weight-normal">@Model.NumberOfSettings</p>
            </div>
        }
        else
        {
            <div class="container row">
                <p class="font-weight-bold">Service Health:  </p>
                <br />
                <p class="font-weight-normal">@Model.SettingServiceHealth</p>
            </div>
        }
        <h3 class="h3">Scan Service</h3>

        <h3 class="h3">Device Service</h3>

    </div>

